local _unc = {hookfunction=typeof(hookfunction)=="function",Drawing=typeof(Drawing)=="table",writefile=typeof(writefile)=="function",readfile=typeof(readfile)=="function",isfile=typeof(isfile)=="function",makefolder=typeof(makefolder)=="function",require=typeof(require)=="function",getrawmetatable=typeof(getrawmetatable)=="function",setreadonly=typeof(setreadonly)=="function",newcclosure=typeof(newcclosure)=="function",getgc=typeof(getgc)=="function",mouse1press=typeof(mouse1press)=="function",mouse1release=typeof(mouse1release)=="function",listfiles=typeof(listfiles)=="function",delfile=typeof(delfile)=="function"}
local function _gs() local e,d={},{} for f,s in pairs(_unc) do table.insert(s and e or d,f) end return e,d end
local _svc=setmetatable({},{__index=function(t,k) local v=game:GetService(k) rawset(t,k,v) return v end})
local _P,_R,_D=_svc.Players,_svc.RunService,_svc.Debris
local _U,_W,_L=_svc.UserInputService,_svc.Workspace,_svc.Lighting
local _C,_RS,_H=_svc.ContentProvider,_svc.ReplicatedStorage,_svc.HttpService
local lplr=_P.LocalPlayer
local cam=workspace.CurrentCamera
local mouse=lplr:GetMouse()
local _rnd=math.random
local _tick=tick
local _task=task
local _pcall=pcall
local _pairs=pairs
local _ipairs=ipairs
local _type=type
local _typeof=typeof
local _math={random=math.random,floor=math.floor,max=math.max,min=math.min,clamp=math.clamp,sqrt=math.sqrt,exp=math.exp,huge=math.huge,abs=math.abs}
local _string={find=string.find,match=string.match,gsub=string.gsub,format=string.format,lower=string.lower}
local _table={insert=table.insert,sort=table.sort,concat=table.concat}
local function _rng(n) return n+(_rnd(-3,3)*0.001) end
local function _jitter() _task.wait(_rnd()/1000) end
local pInv=_RS:WaitForChild("Players"):WaitForChild(lplr.Name):WaitForChild("Inventory")
local iList=_RS:WaitForChild("ItemsList")
local aiZones=_W:WaitForChild("AiZones")
local saAvail=false
local sa={Enabled=false,WallCheck=true,HitPart="Head",Prediction=true,SharedFOV=true,FOV=100,NoRecoil=false,BulletDrop=true,NoDrop=false,Snaplines=false,NoSpread=false,FreezeTarget=false,BulletCalc=false}
local _bMod,_bOld
local _oDrp={}
local _oSpd={}
local _wrapHook=function(f)
    if _unc.newcclosure then return newcclosure(f) end
    return f
end
local function _safeAttr(obj,attr,val)
    _pcall(function() obj:SetAttribute(attr,val) end)
end
local function _clearAttr(obj,attr)
    _pcall(function() obj:SetAttribute(attr,nil) end)
end
local _dropAttrs={"ProjectileDrop","GravityScale","Gravity","DropRate","BulletDrop","DropAmount","FallSpeed"}
local _spreadAttrs={"Spread","SpreadMax","SpreadMin","HipfireSpread","MovingSpread","AimSpread","SpreadMultiplier"}
if _unc.hookfunction and _unc.require then
    local ok,res=_pcall(function()
        local m=_RS:FindFirstChild("Modules")
        if not m then return nil end
        local f=m:FindFirstChild("FPS")
        if not f then return nil end
        local b=f:FindFirstChild("Bullet")
        if not b then return nil end
        return require(b)
    end)
    if ok and res then _bMod=res saAvail=true end
end
local Cfg={AutoSave=false,SaveInterval=30,QuickToggleKey=Enum.KeyCode.RightControl,PanicKey=Enum.KeyCode.End,NotificationDuration=3,ConfigFolder="SugmaConfigs",CurrentConfig="Default",LastConfig=""}
local _lPos,_lSave=Vector3.zero,_tick()
local espS,espD,iDrw,gList,iconCache,playerColors={},{},{},{},{},{}
local bState={Dozer=false,Whisper=false,Anton=false}
local espAIH={}
local vmAimPart,curInvT,frozenT=nil,nil,nil
local freezeMode,freezeActive="Always",false
local origFog={End=_L.FogEnd,Start=_L.FogStart,Color=_L.FogColor}
local origLight={ClockTime=_L.ClockTime,Brightness=_L.Brightness,GlobalShadows=_L.GlobalShadows,Ambient=_L.Ambient,OutdoorAmbient=_L.OutdoorAmbient}
local selSight,selDev,aimP,sens,fovR,showF,trigM="ACOG","Flashlight","Head",0,100,false,"Hold Key"
local espP,espB,espW,espI,espA,invImgMode=false,false,false,false,false,false
local espNameTag,espBox,espSkel,espVel,espDist,espLook,espRand=false,false,false,false,false,false,false
local fbEn,sfbEn,nvEn,fovZoomOff,isRCZ=false,false,false,false,false
local noFog,zeroVM,iChk,iOff=false,false,false,0
local fmChg,boltOrig,priS={},{},{ItemBack1=1,ItemBack2=1,ItemHip1=1,ClothingHeadware=1,ClothingLegArmor=1,ClothingChestRig=1}
local sTable={"ACOG","ELCAN","HoloSight","LPVO","OKP7","PSO1","Reapir","Scope6X","SniperScope","T1Sight","SRO","PN6K5","T12W"}
local lastTarget,tarG=nil,nil
local Lib,cfgCache={},{}
local conns={}
local fovC,snapDraw
if _unc.Drawing then
    fovC=Drawing.new("Circle")
    fovC.Thickness=2 fovC.Color=Color3.new(1,1,1) fovC.Transparency=0
    fovC.Filled=false fovC.Visible=false fovC.Radius=100
    fovC.Position=Vector2.new(0,0)
    snapDraw=Drawing.new("Line")
    snapDraw.Thickness=1 snapDraw.Color=Color3.new(1,0,0)
    snapDraw.Transparency=1 snapDraw.Visible=false
end
local function notify(t,m,d)
    if Lib and Lib.Notify then Lib:Notify(t,m,d or Cfg.NotificationDuration) end
end
local function _ensureFolder()
    if _unc.makefolder then _pcall(function() if not isfolder(Cfg.ConfigFolder) then makefolder(Cfg.ConfigFolder) end end) end
end
local function _cfgList()
    local c={}
    if _unc.listfiles then
        _ensureFolder()
        local ok,f=_pcall(function() return listfiles(Cfg.ConfigFolder) end)
        if ok and f then
            for _,file in _pairs(f) do
                local n=file:match("([^/]+)%.json$")
                if n then _table.insert(c,n) end
            end
        end
    end
    if #c==0 then _table.insert(c,"Default") end
    _table.sort(c)
    return c
end
local function saveConfig(name)
    if not _unc.writefile then notify("Error","writefile unsupported",3) return end
    _ensureFolder()
    local n=name or Cfg.CurrentConfig
    local ok=_pcall(function()
        writefile(Cfg.ConfigFolder.."/"..n..".json",_H:JSONEncode({Flags=Lib.Flags,SilentAim=sa,Timestamp=os.time()}))
    end)
    if ok then Cfg.LastConfig=n notify("Config","Saved: "..n,2) return true
    else notify("Error","Failed to save",3) return false end
end
local function loadConfig(name)
    if not _unc.readfile or not _unc.isfile then notify("Error","File ops unsupported",3) return end
    local n=name or Cfg.CurrentConfig
    local path=Cfg.ConfigFolder.."/"..n..".json"
    if not isfile(path) then notify("Error","Not found: "..n,3) return end
    local ok,data=_pcall(function() return _H:JSONDecode(readfile(path)) end)
    if ok and data then
        if data.Flags then for k,v in _pairs(data.Flags) do Lib.Flags[k]=v end end
        if data.SilentAim then for k,v in _pairs(data.SilentAim) do if sa[k]~=nil then sa[k]=v end end end
        Lib:Update() Cfg.LastConfig=n notify("Config","Loaded: "..n,2) return true
    else notify("Error","Parse failed",3) return false end
end
local function deleteConfig(name)
    if not _unc.delfile then notify("Error","delfile unsupported",3) return end
    local n=name or Cfg.CurrentConfig
    if n=="Default" then notify("Error","Cannot delete Default",3) return end
    local path=Cfg.ConfigFolder.."/"..n..".json"
    if not isfile(path) then notify("Error","Not found: "..n,3) return end
    local ok=_pcall(function() delfile(path) end)
    if ok then notify("Config","Deleted: "..n,2) return true
    else notify("Error","Delete failed",3) return false end
end
local function _alive(c)
    return c and c:FindFirstChild("HumanoidRootPart") and c:FindFirstChild("Humanoid") and c.Humanoid.Health>0
end
local function _visible(origin,target,...)
    local ign={cam,...}
    if _alive(lplr.Character) then _table.insert(ign,lplr.Character) end
    local ray=Ray.new(origin,target.Position-origin)
    local hit=_W:FindPartOnRayWithIgnoreList(ray,ign,false,true)
    return hit and hit:IsDescendantOf(target.Parent)
end
local function getAI()
    local ai={}
    for _,z in _pairs(aiZones:GetChildren()) do
        for _,ch in _pairs(z:GetChildren()) do
            if ch:IsA("Model") and ch:FindFirstChild("Humanoid") then _table.insert(ai,ch) end
        end
    end
    return ai
end
local function getTarget(fov,part,wall)
    local closest,closestD=nil,fov or fovR
    local mp=_U:GetMouseLocation()
    local function _check(char,isPlayer)
        if not _alive(char) then return end
        local tp=char:FindFirstChild(part or aimP)
        if not tp then return end
        if wall and not _visible(cam.CFrame.Position,tp) then return end
        local sp,on=cam:WorldToViewportPoint(tp.Position)
        if not on then return end
        local d=(Vector2.new(sp.X,sp.Y)-mp).Magnitude
        if d<closestD then closestD=d closest=tp end
    end
    for _,ch in _pairs(getAI()) do _check(ch,false) end
    for _,pl in _pairs(_P:GetPlayers()) do
        if pl~=lplr then _check(pl.Character,true) end
    end
    return closest
end
local BC={}
function BC.solveQ(a,b,c)
    local d=b*b-4*a*c
    if d<0 then return nil,nil end
    local s=_math.sqrt(d)
    return(-b-s)/(2*a),(-b+s)/(2*a)
end
function BC.flight(dir,grav,spd)
    local a=grav:Dot(grav)/4
    local b=grav:Dot(dir)-spd*spd
    local c=dir:Dot(dir)
    local t1,t2=BC.solveQ(a,b,c)
    if t1 and t2 then
        if t1>0 and(t2<=0 or t1<t2) then return _math.sqrt(t1) end
        if t2>0 then return _math.sqrt(t2) end
    end
    return nil
end
function BC.drop(origin,tPos,spd,rate)
    local g=Vector3.new(0,rate*2,0)
    local dir=tPos-origin
    local t=BC.flight(dir,g,spd)
    if not t then return Vector3.zero end
    return 0.5*g*(t*t)
end
function BC.effSpd(spd,drag,dist)
    if drag<=0 then return spd end
    return spd*_math.exp(-drag*(dist/spd))
end
function BC.calc(origin,target,bullet,aimPartCF)
    local spd=bullet:GetAttribute("MuzzleVelocity") or 500
    local drop=bullet:GetAttribute("ProjectileDrop") or 0
    local drag=bullet:GetAttribute("Drag") or 0
    local tPos=target.Position
    local dist=(tPos-origin).Magnitude
    local eSpd=BC.effSpd(spd,drag,dist)
    local g=Vector3.new(0,drop*2,0)
    local dir=tPos-origin
    local ft=BC.flight(dir,g,eSpd) or dist/_math.max(eSpd,1)
    local pred=tPos
    local hrp=target.Parent and target.Parent:FindFirstChild("HumanoidRootPart")
    if hrp then pred=tPos+(hrp.AssemblyLinearVelocity*ft) end
    local dir2=pred-origin
    local ft2=BC.flight(dir2,g,eSpd) or ft
    local dOff=Vector3.zero
    if drop~=0 then dOff=0.5*g*(ft2*ft2) end
    local final=pred+dOff
    return CFrame.new(origin,final),final,ft2
end
local function _applyBulletHook()
    if not saAvail then return end
    local _fakeDelay=_rng(0.1)
    _task.wait(_fakeDelay)
    local function _hookImpl(idk,model,model2,model3,aimPart,idk2,ammoType,tick,recoilPattern)
        local bullet=_RS.AmmoTypes:FindFirstChild(ammoType)
        if bullet then
            if not _oDrp[ammoType] then _oDrp[ammoType]=bullet:GetAttribute("ProjectileDrop") end
            if not _oSpd[ammoType] then
                _oSpd[ammoType]={}
                for _,a in _pairs(_spreadAttrs) do _oSpd[ammoType][a]=bullet:GetAttribute(a) end
            end
            if sa.NoDrop then
                for _,a in _pairs(_dropAttrs) do _safeAttr(bullet,a,0) end
            elseif _oDrp[ammoType] then
                _safeAttr(bullet,"ProjectileDrop",_oDrp[ammoType])
            end
            _safeAttr(bullet,"Drag",0)
            if sa.NoRecoil then recoilPattern={} end
            if sa.NoSpread then
                for _,a in _pairs(_spreadAttrs) do _safeAttr(bullet,a,0) end
            elseif _oSpd[ammoType] then
                for k,v in _pairs(_oSpd[ammoType]) do if v~=nil then _safeAttr(bullet,k,v) end end
            end
        end
        local tFOV=sa.SharedFOV and fovR or sa.FOV
        local target=getTarget(tFOV,sa.HitPart,sa.WallCheck)
        if sa.Enabled and sa.FreezeTarget and target and target.Parent then
            local hrp=target.Parent:FindFirstChild("HumanoidRootPart")
            if hrp then
                _pcall(function() hrp.Anchored=true end)
                _task.delay(0.5,function() _pcall(function() hrp.Anchored=false end) end)
            end
        end
        if sa.Enabled and target and bullet then
            local origin=aimPart.Position
            local newCF
            if sa.BulletCalc then
                newCF=BC.calc(origin,target,bullet,aimPart)
            else
                local function sQ(A,B,C)
                    local d=B^2-4*A*C
                    if d<0 then return nil,nil end
                    local r=_math.sqrt(d)
                    return(-B-r)/(2*A),(-B+r)/(2*A)
                end
                local function gFT(dir,g,spd)
                    local r1,r2=sQ(g:Dot(g)/4,g:Dot(dir)-spd^2,dir:Dot(dir))
                    if r1 and r2 then
                        if r1>0 and r1<r2 then return _math.sqrt(r1)
                        elseif r2>0 and r2<r1 then return _math.sqrt(r2) end
                    end
                    return 0
                end
                local accel=sa.NoDrop and 0 or(bullet:GetAttribute("ProjectileDrop") or 0)
                local spd=bullet:GetAttribute("MuzzleVelocity") or 500
                local g=Vector3.new(0,accel*2,0)
                local tPos
                if sa.Prediction then
                    local t=gFT(target.Position-origin,g,spd)
                    tPos=target.Position+(target.AssemblyLinearVelocity*t)
                else
                    tPos=target.Position
                end
                local vert=Vector3.zero
                if sa.BulletDrop and not sa.NoDrop then
                    local t2=gFT(tPos-origin,g,spd)
                    vert=0.5*g*t2^2
                end
                newCF=CFrame.new(origin,tPos+vert)
            end
            local fakeAimPart={["CFrame"]=newCF}
            return _bOld(idk,model,model2,model3,fakeAimPart,idk2,ammoType,tick,recoilPattern)
        end
        return _bOld(idk,model,model2,model3,aimPart,idk2,ammoType,tick,recoilPattern)
    end
    _bOld=hookfunction(_bMod.CreateBullet,_wrapHook(_hookImpl))
end
if saAvail then _task.spawn(_applyBulletHook) end
local function _clrEntry(m)
    if not espS[m] then return end
    _pcall(function() if espS[m].B then espS[m].B:Destroy() end end)
    _pcall(function() if espS[m].H then espS[m].H:Destroy() end end)
    if _unc.Drawing then
        _pcall(function() if espS[m].BX then espS[m].BX:Remove() end end)
        _pcall(function() if espS[m].SK then for _,l in _pairs(espS[m].SK) do l:Remove() end end end)
        _pcall(function() if espS[m].VL then espS[m].VL:Remove() end end)
        _pcall(function() if espS[m].LV then espS[m].LV:Remove() end end)
    end
    espS[m]=nil espAIH[m]=nil
end
local function clrEsp() for m in _pairs(espS) do _clrEntry(m) end espS={} espAIH={} end
local function clrDrw()
    for _,d in _pairs(iDrw) do
        if _typeof(d)=="Instance" then _pcall(function() d:Destroy() end)
        elseif d and d.Remove then _pcall(function() d:Remove() end) end
    end
    iDrw={} curInvT=nil
end
local SKEL={{"Head","UpperTorso"},{"UpperTorso","LowerTorso"},{"UpperTorso","LeftUpperArm"},{"LeftUpperArm","LeftLowerArm"},{"LeftLowerArm","LeftHand"},{"UpperTorso","RightUpperArm"},{"RightUpperArm","RightLowerArm"},{"RightLowerArm","RightHand"},{"LowerTorso","LeftUpperLeg"},{"LeftUpperLeg","LeftLowerLeg"},{"LeftLowerLeg","LeftFoot"},{"LowerTorso","RightUpperLeg"},{"RightUpperLeg","RightLowerLeg"},{"RightLowerLeg","RightFoot"}}
local _parts={"Head","UpperTorso","LowerTorso","LeftUpperArm","RightUpperArm","LeftUpperLeg","RightUpperLeg","LeftFoot","RightFoot","LeftHand","RightHand"}
local function _bounds(m)
    local mnX,mnY,mxX,mxY=_math.huge,_math.huge,-_math.huge,-_math.huge
    local vis=false
    for _,pn in _pairs(_parts) do
        local p=m:FindFirstChild(pn)
        if p then
            local sp,on=cam:WorldToViewportPoint(p.Position)
            if on then
                vis=true
                mnX=_math.min(mnX,sp.X) mnY=_math.min(mnY,sp.Y)
                mxX=_math.max(mxX,sp.X) mxY=_math.max(mxY,sp.Y)
            end
        end
    end
    return vis,mnX,mnY,mxX,mxY
end
local function w2s(pos)
    local p,v=cam:WorldToViewportPoint(pos)
    return v,Vector2.new(p.X,p.Y)
end
local function _rndCol(n)
    if not playerColors[n] then playerColors[n]=Color3.fromHSV(_math.random(),0.85,1) end
    return playerColors[n]
end
local function getHld(m)
    if not m then return end
    for _,c in _pairs(m:GetChildren()) do
        if c:IsA("Model") and c.PrimaryPart and c.PrimaryPart.Name=="ItemRoot" then return c.Name end
    end
end
local function getWldModel(m)
    if not m then return end
    for _,c in _pairs(m:GetChildren()) do
        if c:IsA("Model") and (c.Name:find("Holstered") or c.Name:find("Holster") or c.Name:find("holstered") or c.Name:find("holster")) then return c.Name end
    end
end
local function getStack(item)
    local amt=item:GetAttribute("Amount") or item:GetAttribute("Count") or item:GetAttribute("Quantity") or item:GetAttribute("Stack")
    if not amt then
        for _,a in _pairs({"amount","count","quantity","stack","stackSize","StackSize","stackCount"}) do
            local v=item:GetAttribute(a)
            if v and _type(v)=="number" then amt=v break end
        end
    end
    return(_type(amt)=="number" and amt>0) and amt or 1
end
local function fmtItm(i) local a=getStack(i) return a>1 and i.Name.." ["..a.."x]" or i.Name end
local function fmtWep(w)
    local t,l=w.Name,{}
    local f=w:FindFirstChild("Attachments")
    if f then
        for _,a in _pairs(f:GetChildren()) do
            local s=a:GetAttribute("Slot")
            if s=="Sight" or s=="Magazine" or s=="Extra" then
                local n=a.Name
                if s=="Magazine" then n=n.."("..(a:GetAttribute("LoadedAmmo") or 0)..")" end
                _table.insert(l,n)
            end
        end
    end
    return #l>0 and t.." [".._table.concat(l,", ").."]" or t.." [None]"
end
local function isGun(n)
    local i=iList:FindFirstChild(n)
    if i then local ip=i:FindFirstChild("ItemProperties") if ip and ip:GetAttribute("ItemType")=="RangedWeapon" then return true end end
    return false
end
local function getIcon(n) return iconCache[n] end
local function buildInvData(pinv)
    local eq,bp={},{}
    for _,v in _pairs(pinv:GetChildren()) do
        local sl=v:GetAttribute("Slot")
        local ni=v:FindFirstChild("Inventory")
        if sl and priS[sl] then _table.insert(eq,{item=v,slot=sl})
        else _table.insert(bp,v) end
        if ni then for _,k in _pairs(ni:GetChildren()) do _table.insert(bp,k) end end
    end
    return eq,bp
end
local function mkBadge(parent,txt)
    local l=Instance.new("TextLabel",parent)
    l.Size=UDim2.new(1,-2,0,13) l.Position=UDim2.new(0,0,0,0)
    l.BackgroundTransparency=1 l.TextColor3=Color3.fromRGB(255,220,50)
    l.Font=Enum.Font.GothamBold l.TextSize=10 l.Text=txt
    l.TextXAlignment=Enum.TextXAlignment.Right l.TextYAlignment=Enum.TextYAlignment.Top
    l.TextStrokeTransparency=0 l.ZIndex=5
end
local function getAttachIcons(inv)
    local icons={}
    local att=inv:FindFirstChild("Attachments")
    if att then
        for _,a in _pairs(att:GetChildren()) do
            local s=a:GetAttribute("Slot")
            if s=="Sight" or s=="Magazine" or s=="Extra" or s=="Barrel" or s=="UnderBarrel" then
                local ic=getIcon(a.Name)
                if ic then _table.insert(icons,{icon=ic,name=a.Name}) end
            end
        end
    end
    return icons
end
local function drawImageInv(pinv,tName)
    clrDrw()
    local sg=lplr.PlayerGui:FindFirstChild("_SysGui9")or Instance.new("ScreenGui",lplr.PlayerGui)
    sg.Name="_SysGui9" sg.ResetOnSpawn=false sg.IgnoreGuiInset=true
    for _,c in _pairs(sg:GetChildren()) do c:Destroy() end
    _table.insert(iDrw,sg)
    local sz,pad,cols=58,4,6
    local pw=cols*(sz+pad)+pad+10
    local eq,bp=buildInvData(pinv)
    local bpSlice={}
    for i=1+iOff,_math.min(#bp,iOff+cols*8) do _table.insert(bpSlice,bp[i]) end
    local eqH=#eq>0 and(18+_math.ceil(#eq/cols)*(sz+pad)+pad)or 0
    local bpH=18+_math.ceil(_math.max(#bpSlice,1)/cols)*(sz+pad)+pad
    local ph=30+eqH+bpH+10
    local mp=_U:GetMouseLocation()
    local px=_math.clamp(mp.X+16,0,cam.ViewportSize.X-pw-4)
    local py=_math.clamp(mp.Y,0,cam.ViewportSize.Y-ph-4)
    local panel=Instance.new("Frame",sg)
    panel.Size=UDim2.new(0,pw,0,ph) panel.Position=UDim2.new(0,px,0,py)
    panel.BackgroundColor3=Color3.fromRGB(15,15,15) panel.BackgroundTransparency=0.1 panel.BorderSizePixel=0
    local pc=Instance.new("UICorner",panel) pc.CornerRadius=UDim.new(0,10)
    local st=Instance.new("UIStroke",panel) st.Color=Color3.fromRGB(55,55,55) st.Thickness=1 st.Transparency=0
    local ti=Instance.new("TextLabel",panel)
    ti.Size=UDim2.new(1,-8,0,30) ti.Position=UDim2.new(0,8,0,0)
    ti.BackgroundTransparency=1 ti.TextColor3=Color3.fromRGB(255,255,255)
    ti.Font=Enum.Font.GothamBold ti.TextSize=13
    ti.Text="Viewing "..tName.." | Inventory" ti.TextXAlignment=Enum.TextXAlignment.Left
    local cy=30
    local function drawSec(lbl,items,isEq)
        if #items==0 then return end
        local lb=Instance.new("TextLabel",panel)
        lb.Size=UDim2.new(1,-8,0,18) lb.Position=UDim2.new(0,8,0,cy)
        lb.BackgroundTransparency=1 lb.TextColor3=Color3.fromRGB(155,155,155)
        lb.Font=Enum.Font.GothamBold lb.TextSize=11 lb.Text=lbl lb.TextXAlignment=Enum.TextXAlignment.Left
        cy=cy+18
        local col,rowY=0,cy
        for _,entry in _pairs(items) do
            local v=isEq and entry.item or entry
            local ic=getIcon(v.Name)
            local fx=pad/2+col*(sz+pad)
            local sf=Instance.new("Frame",panel)
            sf.Size=UDim2.new(0,sz,0,sz) sf.Position=UDim2.new(0,fx,0,rowY)
            sf.BackgroundColor3=Color3.fromRGB(32,32,32) sf.BackgroundTransparency=0.2 sf.BorderSizePixel=0
            Instance.new("UICorner",sf).CornerRadius=UDim.new(0,8)
            if ic and ic~="" then
                local img=Instance.new("ImageLabel",sf)
                img.Size=UDim2.new(0,sz-8,0,sz-8) img.Position=UDim2.new(0,4,0,4)
                img.BackgroundTransparency=1 img.Image=ic img.ScaleType=Enum.ScaleType.Fit
                if isGun(v.Name) then
                    local ai=getAttachIcons(v)
                    local ax=0
                    for _,a in _pairs(ai) do
                        local aF=Instance.new("Frame",sf)
                        aF.Size=UDim2.new(0,16,0,16) aF.Position=UDim2.new(0,ax,0,sz-18)
                        aF.BackgroundColor3=Color3.fromRGB(18,18,18) aF.BackgroundTransparency=0.15 aF.BorderSizePixel=0
                        Instance.new("UICorner",aF).CornerRadius=UDim.new(0,3)
                        local aI=Instance.new("ImageLabel",aF)
                        aI.Size=UDim2.new(1,0,1,0) aI.BackgroundTransparency=1
                        aI.Image=a.icon aI.ScaleType=Enum.ScaleType.Fit
                        ax=ax+18
                    end
                end
            else
                local nl=Instance.new("TextLabel",sf)
                nl.Size=UDim2.new(1,-2,1,-2) nl.Position=UDim2.new(0,1,0,1)
                nl.BackgroundTransparency=1 nl.TextColor3=Color3.fromRGB(175,175,175)
                nl.Font=Enum.Font.Gotham nl.TextSize=9 nl.Text=v.Name
                nl.TextWrapped=true nl.TextXAlignment=Enum.TextXAlignment.Center nl.TextYAlignment=Enum.TextYAlignment.Center
            end
            mkBadge(sf,"x"..getStack(v))
            col=col+1
            if col>=cols then col=0 rowY=rowY+sz+pad end
        end
        cy=cy+_math.ceil(_math.max(#items,1)/cols)*(sz+pad)+pad
    end
    if #eq>0 then drawSec("── EQUIPPED ──",eq,true) end
    drawSec("── BACKPACK ──",bpSlice,false)
end
local function drawTextInv(pinv,tName)
    clrDrw()
    local mp=_U:GetMouseLocation()
    local s=Vector2.new(mp.X+20,mp.Y)
    local eq,bp=buildInvData(pinv)
    local eqF,bpF={},{}
    for _,e in _pairs(eq) do _table.insert(eqF,{T=(e.slot:find("Back") or e.slot:find("Hip")) and fmtWep(e.item) or fmtItm(e.item),S=e.slot}) end
    for _,v in _pairs(bp) do _table.insert(bpF,fmtItm(v)) end
    local y=0
    local function mk(txt,sz,col)
        local d=Drawing.new("Text")
        d.Text=txt d.Size=sz d.Outline=true d.Color=col
        d.Position=s+Vector2.new(0,y) d.Visible=true
        _table.insert(iDrw,d) y=y+sz+2
    end
    mk(tName,18,Color3.new(1,1,1))
    for _,v in _pairs(eqF) do mk("["..v.S.."] "..v.T,15,Color3.new(0,1,1)) end
    y=y+5 mk("BACKPACK",15,Color3.new(1,0.6,0))
    for i=1+iOff,_math.min(#bpF,iOff+20) do mk(bpF[i],14,Color3.new(0,1,0)) end
end
local function upEsp(m,n,c,w)
    local hum=m and m:FindFirstChild("Humanoid")
    local hrp=m and m:FindFirstChild("HumanoidRootPart")
    if not m or not hrp or not hum then _clrEntry(m) return end
    if hum.Health<=0 then _clrEntry(m) return end
    espAIH[m]=hum.Health
    local myC=lplr.Character
    if not myC or not myC:FindFirstChild("HumanoidRootPart") then return end
    if espRand then c=_rndCol(n) end
    local d=_math.floor((myC.HumanoidRootPart.Position-hrp.Position).Magnitude)
    local hp=_math.floor(hum.Health)
    if not espS[m] then
        local b=Instance.new("BillboardGui",hrp)
        b.AlwaysOnTop=true b.Size=UDim2.new(0,220,0,60) b.StudsOffset=Vector3.new(0,3.5,0)
        b.Adornee=m:FindFirstChild("Head") or hrp
        local l=Instance.new("TextLabel",b)
        l.Size=UDim2.new(1,0,1,0) l.BackgroundTransparency=1
        l.TextColor3=c l.TextStrokeTransparency=0
        l.Font=Enum.Font.GothamBold l.TextSize=13
        local hl=Instance.new("Highlight",m)
        hl.FillColor=c hl.FillTransparency=0.5
        hl.OutlineColor=c hl.Adornee=m
        espS[m]={B=b,L=l,H=hl}
    end
    local e=espS[m]
    e.H.Enabled=true e.H.FillColor=c e.H.OutlineColor=c
    local wldM=getWldModel(m)
    local txt=""
    if espNameTag then txt=n end
    if espDist then txt=txt..(#txt>0 and " | " or "")..d.."m" end
    if w and getHld(m) then txt=txt..(#txt>0 and "\n" or "").."["..getHld(m).."]" end
    if w and wldM then txt=txt..(#txt>0 and "\n" or "").."[W:"..wldM.."]" end
    txt=txt..(#txt>0 and "\n" or "").."HP: "..hp
    e.L.Text=txt e.L.TextColor3=c e.B.Enabled=true
    if espBox and _unc.Drawing then
        local vis,x1,y1,x2,y2=_bounds(m)
        if not e.BX then
            local bf=Drawing.new("Square")
            bf.Filled=false bf.Thickness=1.5 bf.Color=c bf.Transparency=1 bf.Visible=false
            e.BX=bf
        end
        if vis then
            local pad=4
            e.BX.Position=Vector2.new(x1-pad,y1-pad)
            e.BX.Size=Vector2.new((x2-x1)+pad*2,(y2-y1)+pad*2)
            e.BX.Color=c e.BX.Visible=true
        else e.BX.Visible=false end
    elseif e.BX then e.BX:Remove() e.BX=nil end
    if espSkel and _unc.Drawing then
        if not e.SK then e.SK={} end
        local li=1
        for _,pair in _pairs(SKEL) do
            local p1,p2=m:FindFirstChild(pair[1]),m:FindFirstChild(pair[2])
            if p1 and p2 then
                local v1,s1=w2s(p1.Position)
                local v2,s2=w2s(p2.Position)
                if not e.SK[li] then
                    local ln=Drawing.new("Line")
                    ln.Thickness=1 ln.Color=c ln.Transparency=1 ln.Visible=false
                    e.SK[li]=ln
                end
                if v1 and v2 then e.SK[li].From=s1 e.SK[li].To=s2 e.SK[li].Color=c e.SK[li].Visible=true
                else e.SK[li].Visible=false end
                li=li+1
            end
        end
        for i=li,#e.SK do e.SK[i].Visible=false end
    elseif e.SK then
        for _,ln in _pairs(e.SK) do _pcall(function() ln:Remove() end) end
        e.SK=nil
    end
    if espVel and _unc.Drawing then
        if hrp then
            local vel=hrp.AssemblyLinearVelocity
            if vel.Magnitude>0.5 then
                local vis2,sp2=w2s(hrp.Position)
                local _,sp3=w2s(hrp.Position+vel.Unit*3)
                if vis2 then
                    if not e.VL then
                        local vl=Drawing.new("Line")
                        vl.Thickness=2 vl.Color=Color3.new(1,1,0) vl.Transparency=1 vl.Visible=false
                        e.VL=vl
                    end
                    e.VL.From=sp2 e.VL.To=sp3 e.VL.Color=Color3.new(1,1,0) e.VL.Visible=true
                else if e.VL then e.VL.Visible=false end end
            else if e.VL then e.VL.Visible=false end end
        end
    elseif e.VL then _pcall(function() e.VL:Remove() end) e.VL=nil end
    if espLook and _unc.Drawing then
        local head=m:FindFirstChild("Head")
        if head then
            local visH,spH=w2s(head.Position)
            local _,spL=w2s(head.Position+head.CFrame.LookVector*4)
            if visH then
                if not e.LV then
                    local lv=Drawing.new("Line")
                    lv.Thickness=2 lv.Color=c lv.Transparency=1 lv.Visible=false
                    e.LV=lv
                end
                e.LV.From=spH e.LV.To=spL e.LV.Color=c e.LV.Visible=true
            else if e.LV then e.LV.Visible=false end end
        end
    elseif e.LV then _pcall(function() e.LV:Remove() end) e.LV=nil end
end
local function isK(f)
    local k=Lib.Flags and Lib.Flags[f]
    if not k then return false end
    if k=="MouseButton1" then return _U:IsMouseButtonPressed(Enum.UserInputType.MouseButton1)
    elseif k=="MouseButton2" then return _U:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) end
    local s,e=_pcall(function() return Enum.KeyCode[k] end)
    return s and _U:IsKeyDown(e)
end
local function tacTog(s,b)
    if selDev=="None" then return end
    local g=getHld(lplr.Character)
    if not g then return end
    local o=pInv:FindFirstChild(g)
    if not o then return end
    local a=o:FindFirstChild("Attachments") or Instance.new("Folder",o)
    a.Name="Attachments"
    local e
    for _,c in _pairs(a:GetChildren()) do
        if c:GetAttribute("Slot")=="Extra" then e=c break end
    end
    if s then
        if e then e:SetAttribute("Toggle",true)
        else
            local r=iList:FindFirstChild(selDev)
            if r then
                local n=Instance.new("ObjectValue",a)
                n.Name=selDev n.Value=r
                n:SetAttribute("Slot","Extra") n:SetAttribute("Toggle",true) n:SetAttribute("ScriptAdded",true)
            end
        end
    else if e then e:SetAttribute("Toggle",false) end end
    if b then notify("OK",s and "Tac Enabled" or "Tac Disabled",2) end
end
local function shouldFreeze()
    if not Lib.Flags or not Lib.Flags.FreezeTarget then return false end
    if freezeMode=="Always" then return true
    elseif freezeMode=="Hold" then return isK("FreezeTargetKey")
    elseif freezeMode=="Toggle" then return freezeActive end
    return false
end
local function unanchorFrozen()
    if frozenT and frozenT.Character and frozenT.Character:FindFirstChild("HumanoidRootPart") then
        _pcall(function() frozenT.Character.HumanoidRootPart.Anchored=false end)
    end
    frozenT=nil
end
local function getClst()
    local c,s,m=nil,_math.huge,_U:GetMouseLocation()
    for _,v in _pairs(_P:GetPlayers()) do
        if v~=lplr and v.Character and v.Character:FindFirstChild("Humanoid") and v.Character.Humanoid.Health>0 then
            local t=v.Character:FindFirstChild(aimP) or v.Character:FindFirstChild("Torso")
            if t then
                local p,o=cam:WorldToViewportPoint(t.Position)
                if o then
                    local d=(Vector2.new(p.X,p.Y)-m).Magnitude
                    if d<=fovR and d<s then c=v s=d end
                end
            end
        end
    end
    return c
end
local function cleanupAll()
    notify("Cleanup","Reverting all changes...",2)
    for _,conn in _pairs(conns) do if conn and conn.Disconnect then _pcall(function() conn:Disconnect() end) end end
    conns={}
    clrEsp() clrDrw()
    _L.ClockTime=origLight.ClockTime _L.Brightness=origLight.Brightness
    _L.GlobalShadows=origLight.GlobalShadows _L.Ambient=origLight.Ambient _L.OutdoorAmbient=origLight.OutdoorAmbient
    _L.FogEnd=origFog.End _L.FogStart=origFog.Start _L.FogColor=origFog.Color
    for _,item in _pairs(pInv:GetChildren()) do
        local att=item:FindFirstChild("Attachments")
        if att then
            for _,a in _pairs(att:GetChildren()) do
                if a:GetAttribute("ScriptAdded") then a:Destroy() end
            end
        end
    end
    for gn,orig in _pairs(fmChg) do
        local o=pInv:FindFirstChild(gn)
        if o then
            o:SetAttribute("FireMode",orig.FireMode)
            o:SetAttribute("FireModeIndex",orig.FireModeIndex)
            o:SetAttribute("FireModeIndexS",1)
        end
    end
    fmChg={}
    for gn,orig in _pairs(boltOrig) do
        local o=pInv:FindFirstChild(gn)
        if o then
            o:SetAttribute("FireMode",orig.FireMode)
            o:SetAttribute("FireModeIndex",orig.FireModeIndex)
            o:SetAttribute("FireModeIndexS",1)
        end
    end
    boltOrig={}
    for ammoType,orig in _pairs(_oDrp) do
        local bullet=_RS.AmmoTypes:FindFirstChild(ammoType)
        if bullet then
            _safeAttr(bullet,"ProjectileDrop",orig)
            _clearAttr(bullet,"GravityScale")
            _clearAttr(bullet,"Gravity")
        end
    end
    for ammoType,orig in _pairs(_oSpd) do
        local bullet=_RS.AmmoTypes:FindFirstChild(ammoType)
        if bullet then for k,v in _pairs(orig) do if v~=nil then _safeAttr(bullet,k,v) end end end
    end
    unanchorFrozen()
    cam.FieldOfView=70
    if saAvail then sa.Enabled=false end
    if fovC then fovC.Visible=false _pcall(function() fovC:Remove() end) end
    if snapDraw then snapDraw.Visible=false _pcall(function() snapDraw:Remove() end) end
    _G.JesusEnabled=false
    _pcall(function() lplr.PlayerGui.NoInsetGui.MainFrame.ScreenEffects.Visible=true end)
    _pcall(function() local g=lplr.PlayerGui:FindFirstChild("_SysGui9") if g then g:Destroy() end end)
    for i,v in _pairs(espD) do _pcall(function() v:Destroy() end) espD[i]=nil end
    _G.SugmaLoaded=false
    notify("Cleanup","All changes reverted!",2)
end
_table.insert(conns,_P.PlayerRemoving:Connect(function(p) if p.Character then _clrEntry(p.Character) playerColors[p.Name]=nil end end))
for _,p in _pairs(_P:GetPlayers()) do _table.insert(conns,p.CharacterRemoving:Connect(function(char) _clrEntry(char) end)) end
_table.insert(conns,_P.PlayerAdded:Connect(function(p) _table.insert(conns,p.CharacterRemoving:Connect(function(char) _clrEntry(char) end)) end))
_table.insert(conns,_R.Heartbeat:Connect(function()
    local char=lplr.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") or not _G.JesusEnabled then return end
    local hrp=char.HumanoidRootPart
    if(hrp.Position-_lPos).Magnitude<2 then return end
    local rp=RaycastParams.new()
    rp.FilterDescendantsInstances={char} rp.FilterType=Enum.RaycastFilterType.Exclude rp.IgnoreWater=false
    local res=workspace:Raycast(hrp.Position,Vector3.new(0,-6,0),rp)
    if res and res.Material==Enum.Material.Water then
        _lPos=hrp.Position
        local p=Instance.new("Part")
        p.Size=Vector3.new(15,1,15) p.Position=res.Position p.Anchored=true
        p.CanCollide=true p.Material=Enum.Material.ForceField p.CastShadow=false
        p.Parent=workspace _D:AddItem(p,5)
    end
    if Cfg.AutoSave and _tick()-_lSave>Cfg.SaveInterval then
        saveConfig(Cfg.CurrentConfig) _lSave=_tick()
    end
end))
local function _getB(path)
    local c,last=game,nil
    for s in path:gmatch("[^.]+") do
        if s=="workspace" then c=_W
        elseif c and c:FindFirstChild(s) then c=c[s]
        else return nil,nil end
        last=c
    end
    if last then return last:FindFirstChild("Humanoid"),last end
    return nil,nil
end
_task.spawn(function()
    _task.wait(_rnd(1,3))
    Lib=loadstring(game:HttpGet("https://raw.githubusercontent.com/jamiewaymiee-cmyk/SUGMA-LIBRARY/refs/heads/main/SUGMAHACKS",true))()
    if _G.SugmaLoaded then return Lib:Notify("SUGMAHACKS.CC","Prevented double execution.",5) end
    _G.SugmaLoaded=true
    _task.spawn(function()
        for _,v in _pairs(iList:GetChildren()) do
            local p=v:FindFirstChild("ItemProperties")
            if p and p:GetAttribute("ItemType")=="RangedWeapon" then _table.insert(gList,v.Name) end
        end
        _table.sort(gList)
    end)
    _task.spawn(function()
        for _,v in _pairs(iList:GetChildren()) do
            local ip=v:FindFirstChild("ItemProperties")
            if ip then
                local il=ip:FindFirstChild("ItemIcon")
                if il and il:IsA("ImageLabel") and il.Image~="" then
                    local id=il.Image:match("%d+")
                    if id then
                        iconCache[v.Name]="rbxassetid://"..id
                        _pcall(function() _C:PreloadAsync({il}) end)
                    end
                end
            end
        end
    end)
    local enabled,disabled=_gs()
    if #disabled>0 then Lib:Notify("UNC Test","Missing: ".._table.concat(disabled,", "),5) end
    local Gui=Lib:CreateWindow({Title="SUGMAHACKS.CC - V1.3",Settings=false})
    if saAvail then
        local SATab=Gui:CreateTab("Silent Aim")
        SATab:CreateToggle("Enable Silent Aim","SilentAimToggle",false,function(v) sa.Enabled=v end)
        SATab:CreateToggle("Wall Check","SilentWallCheck",true,function(v) sa.WallCheck=v end)
        SATab:CreateToggle("Prediction","SilentPrediction",true,function(v) sa.Prediction=v end)
        SATab:CreateToggle("No Recoil","SilentNoRecoil",false,function(v) sa.NoRecoil=v end)
        SATab:CreateToggle("No Spread","SilentNoSpread",false,function(v) sa.NoSpread=v end)
        SATab:CreateToggle("No Drop","SilentNoDrop",false,function(v)
            sa.NoDrop=v
            for ammoType,_ in _pairs(_oDrp) do
                local bullet=_RS.AmmoTypes:FindFirstChild(ammoType)
                if bullet then
                    if v then for _,a in _pairs(_dropAttrs) do _safeAttr(bullet,a,0) end
                    else _safeAttr(bullet,"ProjectileDrop",_oDrp[ammoType]) end
                end
            end
        end)
        SATab:CreateToggle("Bullet Drop Compensation","SilentBulletDrop",true,function(v) sa.BulletDrop=v end)
        SATab:CreateToggle("Bullet Calc (Physics)","SilentBulletCalc",false,function(v)
            sa.BulletCalc=v
            if v then notify("Bullet Calc","Full physics calculation enabled",2) end
        end)
        SATab:CreateToggle("Snaplines","SilentSnaplines",false,function(v)
            sa.Snaplines=v
            if not v and snapDraw then snapDraw.Visible=false end
        end)
        SATab:CreateToggle("Freeze Target","SilentFreezeTarget",false,function(v) sa.FreezeTarget=v end)
        SATab:CreateDropdown("Hitbox","SilentHitPart",{"Head","UpperTorso","HumanoidRootPart"},function(v) sa.HitPart=v end)
        SATab:CreateToggle("Share FOV with Aimbot","SilentSharedFOV",true,function(v) sa.SharedFOV=v end)
        SATab:CreateToggle("Show FOV","SilentShowFOV",false)
        SATab:CreateSlider("FOV Size","SilentFOV",10,800,100,function(v)
            sa.FOV=v
            if sa.SharedFOV then fovR=v end
        end)
    else
        local SATab=Gui:CreateTab("Silent Aim")
        SATab:CreateLabel("SILENT AIM UNAVAILABLE")
        if not _unc.hookfunction then SATab:CreateLabel("Reason: hookfunction not supported")
        elseif not _unc.require then SATab:CreateLabel("Reason: require not supported")
        else SATab:CreateLabel("Reason: Bullet module not found") end
    end
    local Cmbt=Gui:CreateTab("Aimbot")
    Cmbt:CreateToggle("Lock Aim","AimbotToggle",false)
    Cmbt:CreateToggle("Freeze Target","FreezeTarget",false)
    Cmbt:CreateDropdown("Freeze Target Mode","FreezeTargetMode",{"Always","Hold","Toggle"},function(v) freezeMode=v end)
    Cmbt:CreateKeybind("Freeze Target Key","FreezeTargetKey",Enum.KeyCode.F,function()
        if freezeMode=="Toggle" then freezeActive=not freezeActive notify("Freeze Target",freezeActive and "Enabled" or "Disabled",2) end
    end)
    Cmbt:CreateToggle("Zero Aimbot With Viewmodel","ZeroAimVM",false,function(v) zeroVM=v if not v then vmAimPart=nil end end)
    Cmbt:CreateKeybind("Aim Key","AimKey",Enum.UserInputType.MouseButton2)
    Cmbt:CreateDropdown("Hitbox","AimPart",{"Head","UpperTorso","HumanoidRootPart"},function(v) aimP=v end)
    Cmbt:CreateSlider("Smoothness (0=Raw)","AimSens",0,0.99,0,function(v) sens=v end)
    Cmbt:CreateToggle("Draw FOV","ShowFOV",false)
    Cmbt:CreateSlider("FOV Size","FOVRad",10,800,100,function(v)
        fovR=v
        if sa.SharedFOV then sa.FOV=v end
    end)
    Cmbt:CreateToggle("Auto Fire","TriggerBot",false)
    Cmbt:CreateDropdown("Fire Mode","TriggerMode",{"Hold Key","Always Auto"},function(v) trigM=v end)
    Cmbt:CreateKeybind("Fire Key","TriggerKey",Enum.UserInputType.MouseButton1)
    local EspTab=Gui:CreateTab("ESP")
    EspTab:CreateToggle("Player Chams","PlayerESP",false,function(v) espP=v if not v then clrEsp() end end)
    EspTab:CreateToggle("Weapon ESP","WeaponESP",false,function(v) espW=v end)
    EspTab:CreateToggle("Boss Chams","BossESP",false,function(v) espB=v if not v then clrEsp() end end)
    EspTab:CreateToggle("AI Chams","AiESP",false,function(v) espA=v if not v then clrEsp() end end)
    EspTab:CreateToggle("Dead Body ESP","DeadEsp",false)
    EspTab:CreateToggle("Name Tag","EspNameTag",false,function(v) espNameTag=v end)
    EspTab:CreateToggle("Box ESP","EspBox",false,function(v)
        espBox=v
        if not v and _unc.Drawing then for _,e in _pairs(espS) do if e.BX then e.BX:Remove() e.BX=nil end end end
    end)
    EspTab:CreateToggle("Skeleton ESP","EspSkeleton",false,function(v)
        espSkel=v
        if not v and _unc.Drawing then for _,e in _pairs(espS) do if e.SK then for _,l in _pairs(e.SK) do _pcall(function() l:Remove() end) end e.SK=nil end end end
    end)
    EspTab:CreateToggle("Velocity Arrow","EspVelocity",false,function(v)
        espVel=v
        if not v and _unc.Drawing then for _,e in _pairs(espS) do if e.VL then _pcall(function() e.VL:Remove() end) e.VL=nil end end end
    end)
    EspTab:CreateToggle("Look Vector","EspLookVec",false,function(v)
        espLook=v
        if not v and _unc.Drawing then for _,e in _pairs(espS) do if e.LV then _pcall(function() e.LV:Remove() end) e.LV=nil end end end
    end)
    EspTab:CreateToggle("Distance","EspDistance",false,function(v) espDist=v end)
    EspTab:CreateToggle("Randomize Colors","EspRandColor",false,function(v) espRand=v if not v then playerColors={} end end)
    local Vis=Gui:CreateTab("Visuals")
    Vis:CreateToggle("FullBright","Fullbright",false,function(v)
        fbEn=v
        if not v then
            _L.ClockTime=origLight.ClockTime _L.Brightness=origLight.Brightness
            _L.GlobalShadows=origLight.GlobalShadows _L.Ambient=origLight.Ambient _L.OutdoorAmbient=origLight.OutdoorAmbient
        end
    end)
    Vis:CreateToggle("Soft FullBright","SoftFullbright",false,function(v)
        sfbEn=v
        if not v and not fbEn then
            _L.ClockTime=origLight.ClockTime _L.Brightness=origLight.Brightness
            _L.GlobalShadows=origLight.GlobalShadows _L.Ambient=origLight.Ambient _L.OutdoorAmbient=origLight.OutdoorAmbient
        end
    end)
    Vis:CreateSlider("World Time","TimeVal",0,24,12)
    Vis:CreateToggle("Remove Visor","NoVisor",false,function(v) nvEn=v end)
    Vis:CreateToggle("No Fog","NoFog",false,function(v)
        noFog=v
        if v then _L.FogEnd=1e6 _L.FogStart=1e6
        else _L.FogEnd=origFog.End _L.FogStart=origFog.Start _L.FogColor=origFog.Color end
    end)
    Vis:CreateToggle("Enable FOV Changer","EnableFOV",false)
    Vis:CreateSlider("Camera FOV","CamFOV",10,120,70)
    Vis:CreateSlider("Zoom FOV Amount","ZoomFOV",1,120,30)
    Vis:CreateKeybind("Zoom Key","ZoomKey",Enum.KeyCode.Z)
    Vis:CreateToggle("Allow ADS Zoom (RMB)","AllowADS",true,function(v) fovZoomOff=not v end)
    local PlyTab=Gui:CreateTab("Player")
    PlyTab:CreateToggle("Dynamic Speedhack","DynSpeed",false)
    PlyTab:CreateToggle("Tpwalk","TpWalk",false)
    PlyTab:CreateSlider("Tpwalk Speed","TpWalkSpeed",0,0.3,0.05)
    PlyTab:CreateToggle("Speedhack","Speedhack",false)
    PlyTab:CreateToggle("Jesus Mode","Jesus",false,function(v) _G.JesusEnabled=v end)
    local InvTab=Gui:CreateTab("InvChecker")
    InvTab:CreateToggle("InvViewer","InvViewer",false,function(v)
        espI=v
        if not v then
            clrDrw() iChk=false
            _pcall(function() local g=lplr.PlayerGui:FindFirstChild("_SysGui9") if g then g:Destroy() end end)
        end
    end)
    InvTab:CreateToggle("Image Mode","InvImageMode",false,function(v)
        invImgMode=v clrDrw()
        _pcall(function() local g=lplr.PlayerGui:FindFirstChild("_SysGui9") if g then g:Destroy() end end)
    end)
    InvTab:CreateKeybind("View Key","InvKey",Enum.KeyCode.V)
    local Mod=Gui:CreateTab("Gun Mod")
    Mod:CreateDropdown("Scope Type","SelectedSight",sTable,function(v) selSight=v end)
    Mod:CreateButton("Force Scope (Held)",function()
        local g=getHld(lplr.Character)
        if not g then return notify("FAIL","Hold Gun!",2) end
        local o=pInv:FindFirstChild(g)
        if not o then return end
        local a=o:FindFirstChild("Attachments")
        if not a then a=Instance.new("Folder",o) a.Name="Attachments" end
        for _,c in _pairs(a:GetChildren()) do
            if c:GetAttribute("Slot")=="Sight" then
                if c:GetAttribute("ScriptAdded") then c:Destroy()
                else return notify("SAFE","Server Scope Detected",3) end
            end
        end
        local r=iList:FindFirstChild(selSight)
        if r then
            local n=Instance.new("ObjectValue",a)
            n.Name=selSight n.Value=r
            n:SetAttribute("Slot","Sight") n:SetAttribute("ScriptAdded",true)
            notify("OK","Scope Attached",2)
        end
    end)
    Mod:CreateButton("Clear Scope (Held)",function()
        local g=getHld(lplr.Character)
        if not g then return end
        local o=pInv:FindFirstChild(g)
        if o and o:FindFirstChild("Attachments") then
            for _,c in _pairs(o.Attachments:GetChildren()) do
                if c:GetAttribute("Slot")=="Sight" and c:GetAttribute("ScriptAdded") then c:Destroy() notify("OK","Cleared",2) end
            end
        end
    end)
    local selFM="Semi"
    Mod:CreateDropdown("FireMode","SelectedFireMode",{"Semi","Auto"},function(v) selFM=v end)
    Mod:CreateButton("Set FireMode",function()
        local g=getHld(lplr.Character)
        if not g then return notify("FAIL","Hold Gun!",2) end
        local o=pInv:FindFirstChild(g)
        if not o then return end
        if not fmChg[g] then fmChg[g]={FireMode=o:GetAttribute("FireMode"),FireModeIndex=o:GetAttribute("FireModeIndex")} end
        o:SetAttribute("FireMode",selFM) o:SetAttribute("FireModeIndex",2) o:SetAttribute("FireModeIndexS",2)
        notify("OK","FireMode set to "..selFM,2)
    end)
    Mod:CreateButton("Revert FireMode",function()
        local c=0
        for gn,orig in _pairs(fmChg) do
            local o=pInv:FindFirstChild(gn)
            if o then
                o:SetAttribute("FireMode",orig.FireMode)
                o:SetAttribute("FireModeIndex",orig.FireModeIndex)
                o:SetAttribute("FireModeIndexS",1)
                c=c+1
            end
            fmChg[gn]=nil
        end
        notify("OK","Reverted "..c.." gun(s)",2)
    end)
    Mod:CreateToggle("Semi Bolt Actions","BoltSemiToggle",false,function(v)
        if v then
            for _,item in _pairs(pInv:GetChildren()) do
                if item:GetAttribute("FireMode")=="Bolt Action" and not boltOrig[item.Name] then
                    boltOrig[item.Name]={FireMode=item:GetAttribute("FireMode"),FireModeIndex=item:GetAttribute("FireModeIndex")}
                    item:SetAttribute("FireMode","Semi") item:SetAttribute("FireModeIndex",2) item:SetAttribute("FireModeIndexS",2)
                end
            end
            notify("OK","Bolt Actions set to Semi",2)
        else
            for gn,orig in _pairs(boltOrig) do
                local o=pInv:FindFirstChild(gn)
                if o then
                    o:SetAttribute("FireMode",orig.FireMode)
                    o:SetAttribute("FireModeIndex",orig.FireModeIndex)
                    o:SetAttribute("FireModeIndexS",1)
                end
            end
            boltOrig={} notify("OK","Bolt Actions reverted",2)
        end
    end)
    local Boss=Gui:CreateTab("Boss Intel")
    local lD=Boss:CreateLabel("Dozer: ...")
    local lW=Boss:CreateLabel("Whisper: ...")
    local lA=Boss:CreateLabel("Anton: ...")
    Boss:CreateToggle("Notify Dozer","NotifDozer",true)
    Boss:CreateToggle("Notify Whisper","NotifWhisper",true)
    Boss:CreateToggle("Notify Anton","NotifAnton",true)
    local Msc=Gui:CreateTab("Misc")
    Msc:CreateDropdown("Search for gun","FindGunName",gList,function(v) tarG=v end)
    Msc:CreateButton("Scan Server",function()
        if not tarG then return notify("FAIL","Select Gun",2) end
        local c=0
        for _,p in _pairs(_RS.Players:GetChildren()) do
            if p.Name~=lplr.Name then
                local inv=p:FindFirstChild("Inventory")
                local h=inv and(inv:FindFirstChild(tarG) or false)
                if not h and inv then
                    for _,v in _pairs(inv:GetChildren()) do
                        if v:FindFirstChild("Inventory") and v.Inventory:FindFirstChild(tarG) then h=true break end
                    end
                end
                if h then notify("FOUND",p.Name.." > "..tarG,8) c=c+1 end
            end
        end
        if c==0 then notify("404","None Found",3) end
    end)
    Msc:CreateDropdown("Tac Device","TacDevice",{"Flashlight","LaserPointer","PEQ15","None"},function(v) selDev=v end)
    Msc:CreateButton("Remove Landmines",function()
        local z=_W:FindFirstChild("AiZones")
        if z then
            local c=0
            for _,n in _pairs({"HeliCrashClaymores","OutpostClaymores","OutpostLandmines","ShipWreckClaymores","BridgeClaymores"}) do
                local f=z:FindFirstChild(n)
                if f then for _,x in _pairs(f:GetChildren()) do x:Destroy() c=c+1 end end
            end
            notify("SUCCESS","Removed "..c.." mines.",3)
        else notify("ERROR","AiZones not found.",3) end
    end)
    Msc:CreateToggle("Force Tac","DeviceTog",false,function(v) tacTog(v,true) end)
    Msc:CreateKeybind("Tac Key","TacKey",Enum.KeyCode.T,function()
        local t=not(Lib.Flags.DeviceTog or false)
        Lib.Flags.DeviceTog=t tacTog(t,false)
    end)
    local SettingsTab=Gui:CreateTab("Settings")
    SettingsTab:CreateToggle("Auto-Save Config","AutoSaveToggle",false,function(v)
        Cfg.AutoSave=v
        notify("Auto-Save",v and "Enabled - Saving every "..Cfg.SaveInterval.."s" or "Disabled",2)
    end)
    SettingsTab:CreateSlider("Auto-Save Interval","AutoSaveInterval",10,120,30,function(v) Cfg.SaveInterval=v end)
    local cfgDrop
    _task.spawn(function()
        cfgCache=_cfgList()
        cfgDrop=SettingsTab:CreateDropdown("Select Config","ConfigSelect",cfgCache,function(v) Cfg.CurrentConfig=v end)
    end)
    SettingsTab:CreateTextBoxSend("New Config Name","NewConfigName","",function(content)
        if content and content~="" then
            Cfg.CurrentConfig=content
            if saveConfig(content) then cfgCache=_cfgList() notify("Config","Created: "..content,2) end
        end
    end)
    SettingsTab:CreateButton("Save Current Config",function() saveConfig(Cfg.CurrentConfig) end)
    SettingsTab:CreateButton("Load Selected Config",function() loadConfig(Lib.Flags.ConfigSelect or Cfg.CurrentConfig) end)
    SettingsTab:CreateButton("Delete Selected Config",function()
        local toDel=Lib.Flags.ConfigSelect or Cfg.CurrentConfig
        if deleteConfig(toDel) then
            cfgCache=_cfgList()
            Cfg.CurrentConfig=cfgCache[1] or "Default"
        end
    end)
    SettingsTab:CreateButton("Refresh Config List",function()
        cfgCache=_cfgList()
        notify("Config","Refreshed - "..#cfgCache.." configs found",2)
    end)
    SettingsTab:CreateKeybind("Quick Save Key","QuickSaveKey",Enum.KeyCode.F5,function() saveConfig(Cfg.CurrentConfig) end)
    SettingsTab:CreateButton("Destroy UI",function() cleanupAll() _task.wait(0.1) Lib:UIDelete() end)
    SettingsTab:CreateLabel("UNC Features")
    for feature,status in _pairs(_unc) do SettingsTab:CreateLabel(feature..": "..(status and "YES" or "NO")) end
    SettingsTab:CreateLabel("Silent Aim: "..(saAvail and "Available" or "Unavailable"))
    SettingsTab:CreateLabel("Last Config: "..(Cfg.LastConfig~="" and Cfg.LastConfig or "None"))
    _table.insert(conns,_U.InputChanged:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseWheel and iChk and not invImgMode then
            iOff=i.Position.Z>0 and _math.max(0,iOff-1) or iOff+1
        end
    end))
    _table.insert(conns,_U.InputBegan:Connect(function(i,gp)
        if gp then return end
        if i.UserInputType==Enum.UserInputType.MouseButton2 then isRCZ=true end
        if i.KeyCode==Cfg.PanicKey then
            clrEsp()
            if saAvail then sa.Enabled=false end
            notify("PANIC","All features disabled!",3)
        end
    end))
    _table.insert(conns,_U.InputEnded:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton2 then isRCZ=false end
    end))
    _task.spawn(function()
        while true do
            _task.wait(0.1+(_rnd()/20))
            if zeroVM then
                local ok,r=_pcall(function()
                    local vm=cam:FindFirstChild("ViewModel")
                    if not vm then return nil end
                    local item=vm:FindFirstChildWhichIsA("Model")
                    if not item then return nil end
                    local offsets=item:FindFirstChild("Offsets")
                    if not offsets then return nil end
                    return offsets:FindFirstChild("AimPart")
                end)
                vmAimPart=ok and r or nil
            else vmAimPart=nil end
        end
    end)
    _table.insert(conns,_R.Heartbeat:Connect(function()
        if Lib.Flags.TpWalk and lplr.Character and lplr.Character:FindFirstChild("HumanoidRootPart") and lplr.Character:FindFirstChild("Humanoid") then
            local hrp,hum=lplr.Character.HumanoidRootPart,lplr.Character.Humanoid
            if hum.MoveDirection.Magnitude>0 then
                hrp.CFrame=hrp.CFrame+(hum.MoveDirection*(Lib.Flags.TpWalkSpeed or 0.05))
            end
        end
    end))
    _table.insert(conns,_R.RenderStepped:Connect(function()
        sens=Lib.Flags.AimSens or 0
        fovR=Lib.Flags.FOVRad or 100
        local tVal=Lib.Flags.TimeVal or 12
        if fbEn then
            _L.ClockTime=14 _L.Brightness=2 _L.GlobalShadows=false
            _L.Ambient=Color3.new(1,1,1) _L.OutdoorAmbient=Color3.new(1,1,1)
        elseif sfbEn then
            _L.ClockTime=14 _L.Brightness=1.5 _L.GlobalShadows=true
            _L.Ambient=Color3.new(0.7,0.7,0.7) _L.OutdoorAmbient=Color3.new(0.7,0.7,0.7)
        else
            _L.ClockTime=tVal _L.GlobalShadows=true
        end
        if noFog then _L.FogEnd=1e6 _L.FogStart=1e6 end
        _pcall(function() lplr.PlayerGui.NoInsetGui.MainFrame.ScreenEffects.Visible=not nvEn end)
        if Lib.Flags.Speedhack and lplr.Character and lplr.Character:FindFirstChild("Humanoid") then
            local hum=lplr.Character.Humanoid
            if hum.WalkSpeed>18.2 then hum.WalkSpeed=0 else hum.WalkSpeed=18 end
        end
        if Lib.Flags.DynSpeed and lplr.Character and lplr.Character:FindFirstChild("Humanoid") then
            local ok,st=_pcall(function() return lplr.PlayerGui.MainGui.MainFrame.BackpackFrame.CharacterFrame.VitalSigns.Speed.Number.Text end)
            if ok and st then lplr.Character.Humanoid.WalkSpeed=tonumber(st:gsub("[^%d%.]","")) or 18 end
        end
        if Lib.Flags.DeadEsp then
            local itms=_W:FindFirstChild("DroppedItems")
            if itms then
                for _,v in _pairs(itms:GetChildren()) do
                    if v:IsA("Model") and not espD[v] then
                        for _,p in _pairs(_P:GetPlayers()) do
                            if v.Name:find(p.Name) then
                                local base=v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart")
                                if base then
                                    local b=Instance.new("BillboardGui",base)
                                    b.AlwaysOnTop=true b.Size=UDim2.new(0,200,0,50) b.StudsOffset=Vector3.new(0,2,0)
                                    local l=Instance.new("TextLabel",b)
                                    l.Size=UDim2.new(1,0,1,0) l.BackgroundTransparency=1
                                    l.TextColor3=Color3.new(0.7,0.7,0.7) l.TextStrokeTransparency=0
                                    l.Font=Enum.Font.GothamBold l.TextSize=13
                                    l.Text="DEAD: "..p.Name
                                    espD[v]=b
                                end
                            end
                        end
                    end
                end
            end
        else
            for i,v in _pairs(espD) do _pcall(function() v:Destroy() end) espD[i]=nil end
        end
        if _unc.Drawing and fovC then
            local showA=Lib.Flags and Lib.Flags.ShowFOV
            local showS=Lib.Flags and Lib.Flags.SilentShowFOV and saAvail and sa.Enabled
            if showA or showS then
                fovC.Visible=true fovC.Position=_U:GetMouseLocation() fovC.Transparency=0.7
                if showS and not sa.SharedFOV then fovC.Radius=sa.FOV fovC.Color=Color3.new(0,1,0)
                else fovC.Radius=fovR fovC.Color=Color3.new(1,1,1) end
            else fovC.Visible=false end
        end
        if _unc.Drawing and snapDraw then
            if saAvail and sa.Enabled and sa.Snaplines then
                local tFOV=sa.SharedFOV and fovR or sa.FOV
                local target=getTarget(tFOV,sa.HitPart,sa.WallCheck)
                if target then
                    local sp,on=cam:WorldToViewportPoint(target.Position)
                    if on then
                        snapDraw.From=_U:GetMouseLocation()
                        snapDraw.To=Vector2.new(sp.X,sp.Y)
                        snapDraw.Visible=true
                    else snapDraw.Visible=false end
                else snapDraw.Visible=false end
            else snapDraw.Visible=false end
        end
        if Lib.Flags.EnableFOV then
            if(isRCZ and not fovZoomOff) or isK("ZoomKey") then cam.FieldOfView=Lib.Flags.ZoomFOV or 30
            else cam.FieldOfView=Lib.Flags.CamFOV or 70 end
        end
        local curT=nil
        local sf=shouldFreeze()
        if Lib.Flags.AimbotToggle and isK("AimKey") then
            local t=getClst()
            if t and t.Character then
                curT=t
                local lerpA=sens<=0 and 1 or _math.clamp(1-sens,0.01,1)
                if zeroVM and vmAimPart then
                    local tp=t.Character:FindFirstChild(aimP) or t.Character:FindFirstChild("Torso")
                    if tp then
                        local vmCF=vmAimPart.CFrame
                        local camCF=cam.CFrame
                        local vmOff=camCF:ToObjectSpace(vmCF)
                        local vmPos=vmOff.Position
                        local corrected=tp.Position-Vector3.new(vmPos.X,vmPos.Y,0)
                        cam.CFrame=cam.CFrame:Lerp(CFrame.new(camCF.Position,corrected),lerpA)
                    end
                else
                    local tp=t.Character:FindFirstChild(aimP) or t.Character:FindFirstChild("Torso")
                    if tp then cam.CFrame=cam.CFrame:Lerp(CFrame.new(cam.CFrame.Position,tp.Position),lerpA) end
                end
                if sf and t.Character:FindFirstChild("HumanoidRootPart") then
                    t.Character.HumanoidRootPart.Anchored=true
                    frozenT=t
                end
            end
        end
        if not sf then unanchorFrozen() elseif curT~=frozenT then unanchorFrozen() end
        if Lib.Flags.TriggerBot and(trigM=="Always Auto" or isK("TriggerKey")) then
            if _unc.mouse1press and _unc.mouse1release then
                local t=mouse.Target
                if t and t.Parent then
                    local c=t.Parent:IsA("Accessory") and t.Parent.Parent or t.Parent
                    if c:FindFirstChild("Humanoid") and c~=lplr.Character and c.Humanoid.Health>0 then
                        mouse1press() _task.wait() mouse1release()
                    end
                end
            end
        end
        if espI and isK("InvKey") then
            local t=getClst()
            if t then
                if curInvT~=t then curInvT=t clrDrw() iChk=true end
                iChk=true
                local rd=_RS:FindFirstChild("Players")
                local pinv=rd and rd:FindFirstChild(t.Name) and rd[t.Name]:FindFirstChild("Inventory")
                if pinv then
                    if invImgMode then drawImageInv(pinv,t.Name) else drawTextInv(pinv,t.Name) end
                end
            else
                if iChk then
                    iChk=false clrDrw()
                    _pcall(function() local g=lplr.PlayerGui:FindFirstChild("_SysGui9") if g then g:Destroy() end end)
                end
            end
        else
            if iChk then
                iChk=false clrDrw()
                _pcall(function() local g=lplr.PlayerGui:FindFirstChild("_SysGui9") if g then g:Destroy() end end)
            end
        end
        if espP then for _,v in _pairs(_P:GetPlayers()) do if v~=lplr and v.Character then upEsp(v.Character,v.Name,Color3.new(1,0.2,0.2),espW) end end end
        if espA then
            for _,char in _pairs(getAI()) do
                if _alive(char) then upEsp(char,char.Name,Color3.new(1,1,0),false) else _clrEntry(char) end
            end
            for char,_ in _pairs(espAIH) do
                if not char.Parent or not _alive(char) then _clrEntry(char) end
            end
        end
        for _,b in _pairs({{P="AiZones.Factory.Dozer",N="Dozer",K="Dozer"},{P="AiZones.Whisper.Whisper",N="Whisper",K="Whisper"},{P="AiZones.Sawmill.Anton",N="Anton",K="Anton"}}) do
            local h,m=_getB("workspace."..b.P)
            if m then
                if espB then upEsp(m,b.N,Color3.new(1,0.6,0),false) end
                if not bState[b.K] then
                    bState[b.K]=true
                    if Lib.Flags["Notif"..b.K] then notify("BOSS",b.N.." Spawned!",5) end
                end
            else bState[b.K]=false end
        end
        local dH=_getB("workspace.AiZones.Factory.Dozer")
        lD:Set(dH and(dH.Health>0 and "Dozer: ".._math.floor(dH.Health) or "Dozer: DEAD") or "Dozer: Not Spawned")
        local wH=_getB("workspace.AiZones.Whisper.Whisper")
        lW:Set(wH and(wH.Health>0 and "Whisper: ".._math.floor(wH.Health) or "Whisper: DEAD") or "Whisper: Not Spawned")
        local aH=_getB("workspace.AiZones.Sawmill.Anton")
        lA:Set(aH and(aH.Health>0 and "Anton: ".._math.floor(aH.Health) or "Anton: DEAD") or "Anton: Not Spawned")
    end))
    _ensureFolder()
    Lib:Notify("SUGMAHACKS.CC","V1.3 Loaded.",5)
    Lib:Notify("UNC Test",#enabled.." features available",3)
    if saAvail then Lib:Notify("Silent Aim","Ready to use!",3)
    else Lib:Notify("Silent Aim","Unavailable (Check tab for details)",5) end
end)
